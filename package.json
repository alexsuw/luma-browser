{
  "name": "luma-browser",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Private browser image enhancement with a compact neural network",
  "license": "MIT",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node scripts/vendor.mjs && vite build && node scripts/size.mjs",
    "train": "node scripts/train.mjs",
    "test": "node --test tests/core.test.mjs",
    "test:browser": "playwright test",
    "quality": "node scripts/quality.mjs"
  },
  "dependencies": {
    "libheif-js": "1.23.2"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "@playwright/test": "^1.58.2"
  }
}
